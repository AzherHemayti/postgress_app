import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Client Access Portal | DevDigitalWorld',
  description: 'Premium client ordering dashboard for DevDigitalWorld — submit requests, track orders, and manage your projects.',
  keywords: ['dashboard', 'client portal', 'orders', 'DevDigitalWorld'],
  openGraph: {
    title: 'Client Access Portal | DevDigitalWorld',
    description: 'Client ordering dashboard for DevDigitalWorld',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
